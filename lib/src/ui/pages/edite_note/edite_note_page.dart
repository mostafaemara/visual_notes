import 'package:flutter/material.dart';

class EditeNotePage extends StatelessWidget {
  static const routeName = "/edite-note";
  const EditeNotePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
